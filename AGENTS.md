# AGENTS.md

이 문서는 저장소 전체에 적용되는 공통 작업 원칙을 정의한다.
`frontend/`와 `backend/`의 기술·구조·테스트 세부 규칙은 각 하위 `AGENTS.md`에서 정의한다.

## 1. 저장소 범위

이 저장소는 프론트엔드와 백엔드가 분리된 모노레포다.

```text
/
├── frontend/
│   └── AGENTS.md
├── backend/
│   └── AGENTS.md
└── AGENTS.md
```

작업을 시작하기 전에 변경 범위가 `frontend`, `backend`, 또는 둘 모두인지 먼저 판단한다.
관련 없는 영역은 수정하지 않는다.

하위 디렉터리에서 작업할 때는 루트 규칙과 해당 디렉터리의 `AGENTS.md`를 함께 따른다.

---

## 2. 공통 작업 원칙

- 요청된 동작을 만족하는 최소한의 변경만 한다.
- 기존 아키텍처, 네이밍, 코드 스타일을 우선한다.
- 구현 전에 관련 코드와 테스트를 먼저 확인한다.
- 요구사항, 비즈니스 규칙, API/event 계약을 임의로 변경하지 않는다.
- 관련 없는 리팩토링을 하지 않는다.
- 미래 확장을 가정한 불필요한 추상화를 만들지 않는다.
- 명확한 필요나 요청이 없다면 새 라이브러리나 대체 기술을 임의로 도입하지 않는다.

큰 아키텍처 결정, 데이터 모델 변경, 외부 계약 변경이 필요하면 임의로 결정하지 말고 먼저 사용자에게 알린다.

---

## 3. 개발 흐름

기능 개발과 버그 수정은 기본적으로 TDD 방식으로 진행한다.

1. 요청된 동작을 이해한다.
2. 관련 구현과 테스트를 확인한다.
3. 필요한 테스트를 추가하거나 수정한다.
4. 여기서 멈추고 사용자가 테스트를 확인하고 직접 실행하도록 한다.
5. 사용자가 확인하면 테스트를 통과하기 위한 최소한의 production 코드를 구현한다.
6. 다시 멈추고 사용자가 코드를 확인하고 직접 테스트를 실행하도록 한다.
7. 필요한 경우에만 동작 확인 이후 리팩토링한다.

- 사용자가 명시적으로 요청하지 않는 한 테스트 작성과 구현을 한 번에 진행하지 않는다.
- 테스트 작성 단계에서는 production 코드를 수정하지 않는다.
- 구현 단계에서는 합의된 테스트를 임의로 수정하지 않는다.
- 한 단계를 마친 뒤 자동으로 다음 단계까지 진행하지 않는다.

### 기능 단위 audit

큰 기능의 필요한 상태를 모두 GREEN으로 만든 뒤 다음 큰 기능으로 넘어가기 전에 audit를 수행한다.

최소한 다음을 확인한다.

- 외부 계약(API, event 등)과 실제 구현의 일치 여부
- 기능의 실제 사용 흐름이 끝까지 연결되는지
- 테스트 누락, 불필요한 중복, 실제 구현을 놓치는 테스트 구조
- cache, lifecycle, persistence 등 기능에 중요한 상태 전이가 누락되지 않았는지
- 다음 기능으로 넘어가기 전에 반드시 수정해야 할 blocker가 있는지

필수 문제를 먼저 보완하고 다시 확인한다. 선택적 개선은 현재 기능 완료를 막지 않으면 별도로 남길 수 있다.

---

## 4. 공통 테스트 원칙

테스트는 내부 구현보다 외부에서 관찰 가능한 동작과 계약을 중심으로 작성한다.

### 우선한다

- 명확한 테스트 이름
- 각 테스트가 자신의 고유한 동작과 결과를 검증하도록 구성
- 필요한 경우 Arrange / Act / Assert 구조 사용
- 반복 케이스의 parameterized test
- 관련 테스트가 여러 개이고 상태, lifecycle, 시나리오처럼 의미 있는 공통 맥락이 있으면 `describe`로 그룹화해 테스트 구조가 드러나게 구성
- `describe` 중첩은 필요한 최소 수준으로 유지하고, 테스트 이름은 상위 `describe`와 중복되는 문맥을 반복하지 않음
- 최소한의 mocking과 stubbing
- 기존 helper와 fixture 재사용
- 반복 boilerplate는 실제 반복이 있을 때 작은 test helper로 추출
- race, retry, cancellation처럼 순서나 타이밍 자체가 계약이면 필요한 시점과 호출 횟수 검증 유지

비동기 시나리오에서 Act와 Assert가 시간 순서에 따라 반복되면 억지로 세 구획으로 나누지 않는다. 필요하면 `Act: ...`, `Assert: ...`처럼 짧은 단계 주석을 사용한다.

### 피한다

- private 구현 세부사항 검증
- 다른 테스트가 이미 충분히 보장하는 중복 assertion이나 중복 시나리오
- 단순히 테스트 수를 나누기 위한 형식적인 `describe` 그룹화나 불필요하게 깊은 중첩
- 테스트 목적과 관계없는 호출 횟수, 내부 listener 등록 방식, 전달 옵션 등의 검증
- 불필요한 stubbing
- 테스트 대상보다 복잡한 test double
- helper/fixture 때문에 테스트의 중요한 입력값이나 상태 차이가 숨겨지는 추상화
- 작은 단위 테스트로 충분한데 불필요하게 큰 통합 테스트 작성
- 테스트 작성 중 production 코드 수정

Test double / stub은 현재 테스트에 필요한 동작만 구현하고 외부 API 전체를 불필요하게 모사하지 않는다.

새 테스트는 주변 테스트 코드의 기존 패턴을 따른다.
사용자가 명시적으로 요청하지 않는 한 테스트를 실행하지 않는다.

---

## 5. Frontend + Backend 동시 작업

하나의 기능이 양쪽 모두에 영향을 주는 경우:

1. 먼저 API 또는 WebSocket event 계약을 확인하거나 정한다.
2. 백엔드 동작을 구현하고 검증한다.
3. 확정된 계약을 기준으로 프론트엔드를 연결한다.
4. 변경 범위를 해당 기능 안으로 제한한다.

계약에 없는 동작을 어느 한쪽에서 임의로 가정하지 않는다.

---

## 6. 리팩토링

가능하면 동작 변경과 리팩토링을 분리한다.

리팩토링은 기존 동작과 계약을 유지하면서 다음과 같은 작은 개선을 우선한다.

- 중복 제거
- 네이밍 정리
- 반복 test boilerplate 정리
- 필요한 범위의 helper 추출
- 반복 테스트의 parameterized test 전환
- 관련 테스트 그룹화

명시적인 논의 없이 큰 아키텍처 리팩토링을 하지 않는다.

---

## 7. 작업 범위

사용자가 요청한 범위를 엄격하게 따른다.

- **조사 / 분석**: 코드만 확인하고 결과를 보고한다. 파일은 수정하지 않는다.
- **테스트 작성**: 테스트 코드만 수정한다.
- **구현**: 합의된 테스트를 만족하기 위해 필요한 production 코드만 수정한다.
- **리팩토링**: 기존 동작을 유지한다.
- **리뷰**: 문제점과 개선안을 보고하고, 요청이 없다면 파일을 수정하지 않는다.

---

## 8. 작업 보고

중요한 변경 전에 간단히 다음을 알린다.

- 확인한 내용
- 관련 파일 또는 영역
- 변경하려는 내용

작업 후에는 간단히 다음을 정리한다.

- 수정한 파일
- 추가하거나 변경한 동작
- 중요한 결정 또는 남은 문제

보고는 간결하게 유지한다.
사용자가 요청하지 않는 한 별도의 Markdown 보고 파일을 만들지 않는다.

---

## 9. 우선순위

지시가 충돌할 경우 다음 우선순위를 따른다.

1. 사용자의 명시적인 지시
2. `frontend/AGENTS.md`, `backend/AGENTS.md` 같은 하위 디렉터리의 더 구체적인 지시
3. 루트 `AGENTS.md`
4. 기존 저장소의 코드 및 테스트 관례
