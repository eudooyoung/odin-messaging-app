# AGENTS.md

## 프로젝트 구조

이 저장소는 프론트엔드와 백엔드가 분리된 모노레포다.

```text
/
├── frontend/
├── backend/
└── AGENTS.md
```

작업을 시작하기 전에 해당 작업이 `frontend`, `backend`, 또는 둘 모두에 해당하는지 먼저 판단한다.

관련 없는 영역은 수정하지 않는다.

---

## 프로젝트 스택

### Backend

- Node.js
- Express
- PostgreSQL
- Prisma
- ESM
- JWT Access Token + Refresh Token
- Argon2id
- `ws`
- Zod
- Vitest + Supertest

### Frontend

- React
- Vite
- React Router
- TanStack Query
- native WebSocket API
- React Hook Form
- Zod
- Tailwind CSS
- Vitest
- React Testing Library
- `@testing-library/user-event`

현재 합의된 스택을 우선한다.

명확한 요청이나 필요가 없다면 새 라이브러리나 대체 기술을 임의로 도입하지 않는다.

---

## 기본 작업 원칙

- 요청된 동작을 만족하는 최소한의 변경만 한다.
- 기존 아키텍처, 네이밍, 코드 스타일을 유지한다.
- 관련 없는 리팩토링을 하지 않는다.
- 구현 전에 관련 코드와 테스트를 먼저 확인한다.
- 현재 코드베이스의 일관성을 우선한다.
- 요구사항이나 비즈니스 규칙을 임의로 변경하지 않는다.
- 미래 확장을 가정한 불필요한 추상화를 만들지 않는다.
- ESM을 기준으로 작성한다.

큰 아키텍처 결정, 데이터 모델 변경, API 계약 변경이 필요하면 임의로 결정하지 말고 먼저 사용자에게 알린다.

---

## 개발 흐름

기능 개발과 버그 수정은 기본적으로 TDD 방식으로 진행한다.

1. 요청된 동작을 이해한다.
2. 관련 구현과 테스트를 확인한다.
3. 필요한 테스트를 추가하거나 수정한다.
4. 여기서 멈추고 사용자가 테스트를 확인하고 직접 실행하도록 한다.
5. 사용자가 확인하면 테스트를 통과하기 위한 최소한의 production 코드를 구현한다.
6. 다시 멈추고 사용자가 코드를 확인하고 직접 테스트를 실행하도록 한다.
7. 필요한 경우에만 동작 확인 이후 리팩토링한다.

사용자가 명시적으로 요청하지 않는 한 테스트 작성과 구현을 한 번에 진행하지 않는다.

테스트 작성 단계에서는 production 코드를 수정하지 않는다.

구현 단계에서는 합의된 테스트를 임의로 수정하지 않는다.

---

## 테스트

테스트는 내부 구현보다 외부에서 관찰 가능한 동작을 중심으로 작성한다.

다음을 우선한다.

- 명확한 테스트 이름
- 필요한 경우 Arrange / Act / Assert 구조
- 기존 helper와 fixture 재사용
- 반복 케이스의 parameterized test
- 관련 테스트 그룹화
- 최소한의 mocking과 stubbing

다음을 피한다.

- 불필요한 stubbing
- private 구현 세부사항 테스트
- 기존 테스트와 중복되는 테스트
- 작은 단위 테스트로 충분한데 불필요하게 큰 통합 테스트 작성
- 테스트 작성 중 production 코드 수정

새 테스트는 주변 테스트 코드의 기존 패턴을 따른다.

사용자가 명시적으로 요청하지 않는 한 테스트를 실행하지 않는다.

---

## Frontend

`frontend/` 작업 시:

- 기존 컴포넌트, hook, 라우팅, 스타일링 규칙을 따른다.
- 서버 상태는 기본적으로 TanStack Query로 관리한다.
- 폼 상태는 React Hook Form, validation은 Zod를 우선한다.
- WebSocket 실시간 이벤트와 REST 기반 서버 상태의 책임을 구분한다.
- WebSocket 이벤트를 TanStack Query cache에 반영할 때 기존 query 구조를 존중한다.
- 사용자에게 보이는 동작을 중심으로 테스트한다.
- 불필요한 컴포넌트 추상화를 피한다.
- semantic HTML과 접근성을 유지한다.
- 명시적인 요청 없이 백엔드 API 계약을 변경하거나 가정하지 않는다.

---

## Backend

`backend/` 작업 시:

- 기존 Express 구조와 모듈 경계를 먼저 확인하고 따른다.
- 특정 계층 구조를 새로 강제하지 않는다.
- 비즈니스 규칙은 route handler에 과도하게 섞지 않고 현재 구조에서 적절한 위치에 둔다.
- request `body`, `query`, `params` validation은 Zod를 우선한다.
- 인증은 합의된 JWT Access Token + Refresh Token 구조를 따른다.
- 비밀번호는 Argon2id를 사용한다.
- Prisma schema와 migration은 사용자 합의 없이 변경하지 않는다.
- 기존 API 계약과 HTTP status 규칙을 유지한다.
- REST API와 WebSocket 이벤트의 책임을 구분한다.
- WebSocket event 이름과 payload를 임의로 변경하지 않는다.
- 불필요한 query나 persistence 추상화를 추가하지 않는다.

---

## Frontend + Backend 동시 작업

하나의 기능이 양쪽 모두에 영향을 주는 경우:

1. 먼저 API 또는 WebSocket event 계약을 확인하거나 정한다.
2. 백엔드 동작을 구현하고 검증한다.
3. 확정된 계약을 기준으로 프론트엔드를 연결한다.
4. 변경 범위를 해당 기능 안으로 제한한다.

계약에 없는 동작을 어느 한쪽에서 임의로 가정하지 않는다.

---

## 리팩토링

가능하면 동작 변경과 리팩토링을 분리한다.

Codex에 맡기기 적합한 작업:

- 반복적인 테스트 boilerplate 작성
- 반복 테스트의 parameterized test 전환
- 관련 테스트 그룹화
- 반복되는 test helper 추출
- 네이밍 정리
- 중복 제거
- 작은 기계적 리팩토링

Codex 지시문은 이 `AGENTS.md`와 적용되는 하위 `AGENTS.md`의 규칙을 이미 전제로 한다.
따라서 TDD 단계, 테스트 실행 여부, 수정 범위 제한처럼 문서에 이미 명시된 공통 제약을 지시문마다 반복하지 않고, 현재 작업의 구체적인 목표와 예외적인 추가 제약만 적는다.

명시적인 논의 없이 큰 아키텍처 리팩토링을 하지 않는다.

---

## 작업 범위

사용자가 요청한 범위를 엄격하게 따른다.

- **조사 / 분석**: 코드만 확인하고 결과를 보고한다. 파일은 수정하지 않는다.
- **테스트 작성**: 테스트 코드만 수정한다.
- **구현**: 합의된 테스트를 만족하기 위해 필요한 production 코드만 수정한다.
- **리팩토링**: 기존 동작을 유지한다.
- **리뷰**: 문제점과 개선안을 보고하고, 요청이 없다면 파일을 수정하지 않는다.

한 단계를 마친 뒤 자동으로 다음 단계까지 진행하지 않는다.

---

## 작업 보고

중요한 변경 전에 간단히 다음을 알린다.

- 확인한 내용
- 관련 파일 또는 영역
- 변경하려는 내용

작업 후에는 간단히 다음을 정리한다.

- 수정한 파일
- 추가하거나 변경한 동작
- 중요한 결정 또는 남은 문제

보고는 간결하게 유지한다.

사용자가 요청하지 않는 한 별도의 Markdown 보고 파일을 만들지 않는다.

---

## 우선순위

지시가 충돌할 경우 다음 우선순위를 따른다.

1. 사용자의 명시적인 지시
2. `frontend/AGENTS.md`, `backend/AGENTS.md` 같은 하위 디렉터리의 더 구체적인 지시
3. 루트 `AGENTS.md`
4. 기존 저장소의 코드 및 테스트 관례
